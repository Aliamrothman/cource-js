// Сделайте код программы короче, используя инкремент.

let index = 0 // Счетчик


let runners = ["Денис", "Виктория", "Антон", "Лена", "Никита"];

for (let index = 0; index < runners.length; index++) {
    console.log(index + 1, runners[index]);
}
